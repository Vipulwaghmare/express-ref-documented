import app from './app';

// let PORT = process.env.PORT || 8080;

// app.listen(PORT, () => {
// 	console.log(`App is running at ${PORT}`);
// });

app.listen(3000, () => {
	console.log("APP IS RUNNING")
})